<h2>Editing Book</h2>

  <%= form_with model: @book,local:true do |f|%>

    <h4>Title</h4>
      <%= f.text_field:title %>

    <h4>body</h4>
      <%= f.text_area:body %>

     <br><%= f.submit'Update Book'%>
  <% end %>
     <br><p><%= link_to "show",book_path(@book.id) %>|<%= link_to "back",books_path %></p>

<% if @book.errors.any? %>
  <%= @book.errors.count%>error prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message|%>
   <%= message %>
<% end %>

 <% end %>